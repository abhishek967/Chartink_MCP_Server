# Chartink credentials (required for authenticated access)
CHARTINK_EMAIL=
CHARTINK_PASSWORD=

# Server
HOST=0.0.0.0
PORT=8000
ENVIRONMENT=production
DEBUG=false
LOG_LEVEL=INFO

# Writable storage (cookies, SQLite). Local: data/  |  Render: /tmp or persistent disk path
DATA_DIR=data

# Automated Chartink login on server (subprocess Playwright; requires email/password)
CHARTINK_AUTO_LOGIN=true
CHARTINK_STARTUP_AUTO_LOGIN=true

# Playwright (required for /refresh-session and scripts/login_test.py)
PLAYWRIGHT_HEADLESS=true
PLAYWRIGHT_TIMEOUT_MS=60000
# Render: set to .playwright-browsers and install in build (see scripts/render_build.sh)
PLAYWRIGHT_BROWSERS_PATH=.playwright-browsers

# Webhook / Cron secrets (set a long random value in production)
WEBHOOK_SECRET=
CRON_SECRET=

# Session refresh interval (minutes)
SESSION_REFRESH_INTERVAL_MINUTES=30

# Default Atlas dashboard for MCP tools
ATLAS_DEFAULT_DASHBOARD=5IvaWealth Advanced

# Daily collector — comma-separated Chartink scan names
COLLECTION_SCAN_NAMES=Volume 3X Weekly,Volume 3X Daily,Trend Template,EPS & Sales Growth
# Optional name:slug pairs so friendly names resolve without discovery
COLLECTION_SCAN_SLUGS=Volume 3X Weekly:volume-3-times-the-average-on-weekly-tf,Volume 3X Daily:volume-2x-daily,Trend Template:trend-template,EPS & Sales Growth:eps-sales-growth
COLLECTION_SCAN_DELAY_SECONDS=1.0
